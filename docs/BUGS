- slurry, cut to the chase mode: PageDown: move to the 1st cell that is invisible instead of showing the entire cell
- The first prioritized dictionary title does not have attribute 'td align="center"' and therefore is not centered (e.g., "перегонка нефти")
- Blocks from '<tr>̈ɪkʌt</tr><em>гл.</em>' (sdict_EnRu_full - cut (manual).txt) are not separated when sorting
- prioritized dictionary titles can be lost in the "Cut to the chase" mode, e.g.: "set" (EN-RU) -> Существительное -> Техника -> ширина развода or Существительное -> Общая лексика -> стойка (собаки). These items are further lost when returning back to a previous mode.
- vertical view, EN-RU: "поцелуй": fix Moves.up for the 1st element
- for vertical view: concerning coloring, rely on row numbers instead of column numbers
- dressing: в MT (браузер) - "противопригарная противопригарный припыл", в mclient - "припыл противопригарная". Вызвано сортировкой по терминам.
- Search dialog appears again after changing the view and pressing F3
- mud: неверный парсинг 'буровой шлам; илистый грунт' -> потеря 'грунт'
- Moves на 'twinkie': работает правильно только при отключенной блокировке
- исправить многоуровневые корректировочные комментарии (e.g., "working documentation"): их можно вычислить по конструкции [correction + Username + Comment + UserName]
- medicine: теряется "сущ." при переключении в режим "Прямо к делу". Если же статья загружается в этом режиме, то "сущ." на месте
- RUS-XAL: липа -> җөөлн цаhан модн -> Общая лексика -> <Нет связи>
- glass bubble: why there are no items in red?
- Some columns disappear when changing views
- (DB) Fix Moves.up on 'painting' (SelectTermsOnly=0)
- (DB) Fix Moves on 'рабочая документация' (SelectTermsOnly=0), selects ')' twice, but does not select corrections
- NODE1 < NODE2 in some rare cases
- ShiftScreen with SelectTermsOnly=0 on 'painting'
- PageDown fails on the last block of MAXCOL (do, set) (we need to loop PgUp/PgDn)
- shift_y does not work properly when changing sizes on-the-fly
- Changing node still sometimes fails
- When adding a space between blocks, add it to the end of the preceding block; otherwise, sorting terms may not work correctly (башмак -> sabaton)
- When deleting/inserting fixed columns, preserve URL
- Clear CurRequest data after clearing history
- Fix links in a saved raw html
- Start ROWNO with 0
- Vertical view (maybe, with Styles): 1st column can be a single entry of 'dic' type
